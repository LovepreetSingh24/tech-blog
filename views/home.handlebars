{{!-- This is the homepage template --}}

{{#each posts}}
  <div class="post">
    <h2>{{this.title}}</h2>
    <p>{{this.content}}</p>
    <!-- Add more content and logic here -->
  </div>
{{else}}
  <p>No posts yet!</p>
{{/each}}
